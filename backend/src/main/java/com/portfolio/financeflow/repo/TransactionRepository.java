package com.portfolio.financeflow.repo;import com.portfolio.financeflow.model.Transaction;import org.springframework.data.jpa.repository.JpaRepository;import java.time.LocalDate;import java.util.*;public interface TransactionRepository extends JpaRepository<Transaction,Long>{List<Transaction>findByDateBetweenOrderByDateDesc(LocalDate from,LocalDate to);List<Transaction>findByTypeIgnoreCase(String type);}
